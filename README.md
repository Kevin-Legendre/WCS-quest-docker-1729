Lancer le projet en mode developpement avec la commande

`docker compose -f docker-compose.dev.yml up --build`

Le frontend est disponible à l'adresse suivante [http://localhost:8080/](http://localhost:8080/)